<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transaction Successful</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fraunces:wght@600;700&family=Source+Sans+3:wght@400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #f5f7fb;
            --ink: #121826;
            --muted: #6b7280;
            --card: #ffffff;
            --accent: #0f5e2f;
            --accent-2: #1f8a4c;
            --ring: rgba(16, 185, 129, 0.18);
            --shadow: 0 18px 60px rgba(15, 23, 42, 0.12);
        }

        * { box-sizing: border-box; }

        body {
            margin: 0;
            min-height: 100vh;
            display: grid;
            place-items: center;
            padding: 32px 16px;
            font-family: "Source Sans 3", "Segoe UI", Tahoma, Arial, sans-serif;
            color: var(--ink);
            background:
                radial-gradient(1200px 600px at 10% 10%, #eef2ff, transparent 55%),
                radial-gradient(1200px 600px at 90% 0%, #ecfeff, transparent 55%),
                var(--bg);
        }

        .card {
            width: min(720px, 100%);
            background: var(--card);
            border-radius: 22px;
            padding: 28px;
            box-shadow: var(--shadow);
            border: 1px solid #eef2f7;
        }

        .header {
            display: flex;
            gap: 16px;
            align-items: center;
            margin-bottom: 12px;
        }

        .icon-wrap {
            width: 64px;
            height: 64px;
            border-radius: 16px;
            background: linear-gradient(135deg, #dcfce7, #ecfdf3);
            display: grid;
            place-items: center;
            border: 1px solid #bbf7d0;
            box-shadow: 0 0 0 6px var(--ring);
        }

        .icon-wrap img {
            width: 34px;
            height: 34px;
        }

        h1 {
            margin: 0;
            font-family: "Fraunces", "Times New Roman", serif;
            font-size: clamp(28px, 3vw, 36px);
            letter-spacing: 0.2px;
        }

        .subtitle {
            margin: 8px 0 18px;
            color: var(--muted);
            font-size: 16px;
        }

        .pill {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 6px 12px;
            border-radius: 999px;
            background: #ecfdf3;
            color: var(--accent);
            font-weight: 600;
            font-size: 13px;
            letter-spacing: 0.4px;
            text-transform: uppercase;
        }

        .actions {
            margin-top: 18px;
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
        }

        .btn {
            padding: 10px 16px;
            border-radius: 10px;
            border: 1px solid transparent;
            font-weight: 600;
            text-decoration: none;
            transition: transform 0.12s ease, box-shadow 0.12s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--accent), var(--accent-2));
            color: #fff;
            box-shadow: 0 10px 20px rgba(16, 185, 129, 0.25);
        }

        .btn-ghost {
            background: #fff;
            color: var(--ink);
            border-color: #e5e7eb;
        }

        .btn:hover { transform: translateY(-1px); }
    </style>
</head>
<body>
    <div class="card">
        <div class="header">
            <div class="icon-wrap">
                <img src="/images/greenTick.png" alt="Payment success tick">
            </div>
            <div>
                <span class="pill">Payment Success</span>
                <h1><%= message %></h1>
            </div>
        </div>
        <p class="subtitle">Thank you for your payment. You can safely close this page or continue shopping.</p>
        <div class="actions">
            <a class="btn btn-primary" href="/payment">Make another payment</a>
            <a class="btn btn-ghost" href="/">Back to Home</a>
        </div>
    </div>
</body>
</html>
